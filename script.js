  // Initialize the carousel
  $('#imageSlider').carousel();

  // Set the interval for automatic sliding (in milliseconds)
  $('#imageSlider').carousel({
      interval: 3000, // Adjust the interval as needed (e.g., 2000 for 2 seconds)
      wrap: true // Enable infinite looping
  });
  
  